# Media File Service Features

Feature folders use the standard backend structure:

```text
<feature>/
  http/
  usecases/
  models/
  repositories/
  clients/
```

Use `.gitkeep` for empty scaffolding until real implementation is added.
